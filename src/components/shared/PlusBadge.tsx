// Roblox Plus tag — animated blue/cyan shimmer rectangle badge. Styles live
// in globals.css (.plus-badge) as one shared keyframe rather than a
// per-instance <style> block, for the same reason DiscountBadge does this.
export default function PlusBadge() {
  return <span className="plus-badge">PLUS</span>
}
